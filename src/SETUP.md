# セットアップガイド

## 📁 プロジェクト構造

```
src/
├── App.jsx                           # ルートコンポーネント
├── components/
│   ├── Dashboard.jsx                 # メインダッシュボード
│   ├── cards/
│   │   ├── MemberCard.jsx           # チームメンバーカード
│   │   ├── RoutineCard.jsx          # ルーティンカード
│   │   └── StatsCard.jsx            # 統計カード
│   ├── modals/
│   │   ├── RoutineDetailModal.jsx   # ルーティン詳細モーダル
│   │   └── TaskDetailModal.jsx      # タスク詳細モーダル
│   └── views/
│       ├── RoutineView.jsx          # ルーティンビュー
│       ├── TeamView.jsx             # チームビュー
│       └── TimelineView.jsx         # タイムラインビュー
├── data/
│   ├── sampleProjects.js            # サンプルプロジェクトデータ
│   ├── sampleRoutines.js            # サンプルルーティンデータ
│   └── sampleTeam.js                # サンプルチームデータ
├── hooks/
│   └── useRoutines.js               # ルーティン管理カスタムフック
└── utils/
    └── colorUtils.js                # カラーユーティリティ関数
```

## 🚀 使い方

### 1. React プロジェクトに統合

```bash
# Reactプロジェクトのルートで
cp -r src/* your-react-project/src/
```

### 2. 必要な依存関係

```bash
npm install lucide-react
```

### 3. Tailwind CSS の設定

`tailwind.config.js`:
```javascript
module.exports = {
  darkMode: 'class',
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

### 4. App.jsx で使用

```javascript
import React from 'react';
import Dashboard from './components/Dashboard';

function App() {
  return <Dashboard />;
}

export default App;
```

## ✨ 主な機能

### タイムラインビュー
- プロジェクトとタスクの一覧表示
- タスククリックで詳細モーダル表示
- 進捗バーによる視覚化

### チームビュー
- メンバーの負荷率と稼働状態
- スキルセットの表示
- チーム全体の統計

### ルーティンビュー（メイン機能）
- 個人/チーム切り替え
- カテゴリー別グループ化（仕事/健康/個人）
- プロジェクト紐付け機能
- 達成率とストリーク表示
- メンバー・プロジェクト別フィルター

## 🎨 カスタマイズ

### カラーの変更

`src/utils/colorUtils.js`:
```javascript
export const getCategoryColor = (category) => {
  const colors = {
    work: 'bg-blue-500',      // お好みの色に変更
    health: 'bg-green-500',
    personal: 'bg-purple-500'
  };
  return colors[category] || 'bg-gray-500';
};
```

### データの変更

`src/data/sampleRoutines.js`を編集して、今日の日付に合わせます：

```javascript
export const sampleRoutines = {
  '2025-10-29': [...]  // ← 今日の日付に変更
};
```

## 🔧 トラブルシューティング

### スタイルが適用されない
Tailwind CSSが正しくセットアップされているか確認してください。

### ルーティンが表示されない
`sampleRoutines.js`の日付が今日の日付になっているか確認してください。

### モジュールが見つからない
```bash
npm install lucide-react
```

## 📝 次のステップ

- LocalStorageでデータ永続化
- ドラッグ&ドロップ機能
- パフォーマンス最適化
- バックエンドAPI連携

## 🎉 完成！

これで4次元プロジェクト管理ダッシュボードの基本構造が完成しました。
各コンポーネントは独立しているため、拡張や修正が簡単です。

Happy coding! 🚀
